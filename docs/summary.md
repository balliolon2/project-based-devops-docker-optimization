## 1. บทนำ (Introduction)

ในโปรเจกต์ Sentinel Home (Log Ingestor) เราพบปัญหาในการ Deployment คือ Image มีขนาดใหญ่เกินความจำเป็น (1.85 GB) ใช้เวลา Build นาน และมีความเสี่ยงด้านความปลอดภัย (Sensitive Data Leakage & Root Execution)

เอกสารฉบับนี้สรุปขั้นตอนการทำ **Refactoring Dockerfile** เพื่อลดขนาด Image และเพิ่มความปลอดภัยตามหลักการ **DevOps Best Practices**

---

## 2. ปัญหาที่พบ (Problem Statement)

จากการวิเคราะห์ Dockerfile เวอร์ชันแรก (`phase1_messy`), พบปัญหาหลัก 3 ประการ:

1. **Size Bloat:** ขนาด Image ใหญ่ถึง **1.85 GB** เนื่องจากการใช้ Base Image ที่ไม่เหมาะสมและการเก็บ Cache ที่ไม่จำเป็น
2. **Security Risk:**
* Application รันด้วยสิทธิ์ **Root** (Default PID 1) ซึ่งเสี่ยงต่อการถูกยึดเครื่อง Host
* ไฟล์ความลับ (`.env`) ถูก Copy ฝังลงไปใน Image โดยตรง


3. **Inefficient Build Process:** ทุกครั้งที่มีการแก้ Source Code, Docker ต้องทำการ Download และ Install Library ใหม่ทั้งหมด ทำให้เสียเวลา (No Layer Caching)

---

## 3. การดำเนินการแก้ไข (Implementation Details)

เราได้ทำการปรับปรุง Dockerfile ใหม่ (`phase2_clean`) โดยใช้เทคนิคดังนี้:

### 3.1 Base Image Selection (เลือกรากฐานที่ถูกต้อง)

* **Before:** `FROM python:3.9` (Full Debian image, heavy)
* **After:** `FROM python:3.9-slim`
* **Reasoning:** เปลี่ยนมาใช้เวอร์ชัน `slim` ซึ่งตัดเครื่องมือที่ไม่จำเป็นออก แต่ยังคง `glibc` ไว้ ทำให้ยังทำงานร่วมกับ Library สาย Data Science (pandas/numpy) ได้ดีกว่า `alpine`

### 3.2 Layer Caching Strategy (เทคนิคการเรียงลำดับ)

เราเปลี่ยนลำดับคำสั่งใน Dockerfile เพื่อใช้ประโยชน์จาก Docker Cache:

1. **Copy `requirements.txt` ก่อน:** เป็นไฟล์ที่เปลี่ยนแปลงน้อยที่สุด
2. **Run `pip install`:** จะถูก Cache ไว้ถาวรตราบใดที่ `requirements.txt` ไม่เปลี่ยน
3. **Copy Source Code ทีหลัง:** เมื่อแก้ Code จะ Re-build เฉพาะ Layer สุดท้ายเท่านั้น (ใช้เวลา < 1 วินาที)

### 3.3 Security Hardening (เสริมเกราะป้องกัน)

* **Non-root User:** สร้าง User ชื่อ `appuser` และสั่ง `USER appuser` เพื่อจำกัดสิทธิ์ของ Container
* **Secret Management:** ใช้ไฟล์ `.dockerignore` เพื่อป้องกันไม่ให้ไฟล์ `.env`, `.git`, และไฟล์ขยะอื่นๆ หลุดเข้าไปใน Image

### 3.4 Cleanup (การกำจัดขยะ)

* เพิ่มคำสั่ง `--no-cache-dir` ใน `pip install` เพื่อไม่ให้เก็บไฟล์ติดตั้ง (.whl) ไว้ใน Image
* ตั้งค่า `ENV PYTHONDONTWRITEBYTECODE=1` เพื่อไม่ให้ Python สร้างไฟล์ `.pyc`

---

## 4. ผลลัพธ์การทดสอบ (Results & Metrics)

เปรียบเทียบประสิทธิภาพระหว่าง `sentinel:messy` และ `sentinel:clean`:

| Metric | Phase 1 (Messy) | Phase 2 (Clean) | Improvement |
| --- | --- | --- | --- |
| **Image Size** | 1.85 GB | **384 MB** | **ลดลง 79.2%** 📉 |
| **Build Time (Re-build)** | ~5 นาที | **< 1 วินาที** | เร็วขึ้นแบบมีนัยสำคัญ ⚡ |
| **User Permission** | Root (Unsafe) | **appuser (Safe)** | ปลอดภัย ✅ |
| **Secrets Exposure** | **Leaked (.env inside)** | **Clean** | ปลอดภัย ✅ |

---

## 5. สิ่งที่ควรรู้สำหรับมือใหม่ (Key Takeaways for Beginners)

จาก Lab นี้ มี 4 คอนเซปต์สำคัญที่ DevOps Engineer ต้องจำให้ขึ้นใจ:

### 5.1 Immutable Infrastructure (โครงสร้างพื้นฐานที่ห้ามแก้)

เมื่อ Build Docker Image เสร็จแล้ว Image นั้นคือ **"สิ่งศักดิ์สิทธิ์"** ห้ามเข้าไปแก้ไฟล์ข้างใน (เช่น ห้าม `docker exec` เข้าไปแก้โค้ดสด)

* **ถ้าจะแก้โค้ด:** ต้องแก้ที่ต้นทาง -> Build Image ใหม่ -> Deploy ใหม่
* **ถ้าจะแก้ Config:** ให้ใช้วิธี "ฉีด" ตัวแปร (Environment Variable) เข้าไปตอนรัน (`docker run -e ...`) แทน

### 5.2 The "Build Context" Awareness

คำสั่ง `COPY . .` อันตรายมากถ้าเราไม่รู้ว่า `.` (จุด) ของเรามีอะไรบ้าง

* **Best Practice:** เสมือนว่าเรากำลังจะส่งไฟล์ให้คนอื่นทาง Email เราต้องเลือกส่งเฉพาะที่จำเป็น การใช้ `.dockerignore` คือการกรองไฟล์ขยะออกก่อนส่ง

### 5.3 Least Privilege Principle (สิทธิ์เท่าที่จำเป็น)

อย่ารันอะไรเป็น Root ถ้าไม่จำเป็น ในโลก Container การเป็น Root ในนั้นอาจหมายถึงการเป็น Root ของเครื่องแม่ได้ถ้ามีการตั้งค่าที่ผิดพลาด

### 5.4 Separation of Concerns (แยกส่วนรับผิดชอบ)

* **Code:** อยู่ใน Git (Repository)
* **Dependencies:** อยู่ใน Image (Docker)
* **Config/Secrets:** อยู่ที่ Environment (Runtime)
อย่าเอา 3 อย่างนี้มายำรวมกันในที่เดียว

---